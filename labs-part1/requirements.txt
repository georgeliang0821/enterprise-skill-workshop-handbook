# 工作坊共用相依清單 —— labs-part1 與 labs-part2 內容完全相同，只需在其中一邊裝一次。
# 安裝（cd 到本資料夾後執行，共用 .venv 在上層）：
#   ..\.venv\Scripts\python.exe -m pip install -r requirements.txt

# Lab 3A — WBS Excel 產出(Copilot 會現場寫 openpyxl 腳本並執行)
openpyxl

# Lab 4 — OpenAPI → FastAPI 骨架
datamodel-code-generator
fastapi
uvicorn[standard]
pydantic

# Lab 5 — Mock Jira Server 與 Skill 的 HTTP 呼叫
requests

# Lab 7 — 雲端 coding agent 產出的測試
pytest
httpx
